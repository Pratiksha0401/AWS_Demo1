pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello World'
            }
        }

        stage('Dev') {
             steps {
                 echo 'Hello World'
                 }
             }

    }
}
