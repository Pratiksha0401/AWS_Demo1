pipeline {
    agent any

    stages {
   
        stage('Dev') {
             steps {
                 echo 'Hello World'
                 }
             }

        stage('Deploy') {
            steps {
                echo 'Hello World'
                 }
         }
    }
}
